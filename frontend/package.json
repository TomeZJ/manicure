{
  "name": "manicure-frontend",
  "private": true,
  "version": "1.0.0",
  "description": "美甲预约系统前端",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:user": "pnpm --filter user-app dev",
    "dev:admin": "pnpm --filter admin-app dev",
    "build:user": "pnpm --filter user-app build",
    "build:admin": "pnpm --filter admin-app build"
  },
  "devDependencies": {
    "pnpm": "^8.15.0"
  }
}
